{
  "name": "last-round",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "check": "node --check game.js && node tests/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
